<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
  <span class="nav-link"><%= link_to 'Welcome', root_path %></span>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <span class="nav-link"><%= link_to 'Home', root_path %></span>
      </li>
      <li class="nav-item">
        <span class="nav-link"><%= link_to 'Articles', articles_path %></span>
      </li>
    </ul>
    <p class="navbar-text float-right">
    <% if author_signed_in? %>
      <strong><%= current_author.username %></strong>.
      <%= link_to 'Edit profile', edit_author_registration_path, :class => 'navbar-link' %> |
      <%= link_to "Logout", destroy_author_session_path, method: :delete, :class => 'navbar-link'  %>
    <% else %>
      <%= link_to "Sign up", new_author_registration_path, :class => 'navbar-link'  %> |
      <%= link_to "Login", new_author_session_path, :class => 'navbar-link'  %>
    <% end %>
    </p>


  </div>
</nav>

<div class="container">
      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>

    <%= yield %>
</div>    
  </body>
</html>
